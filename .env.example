# PRODUCTION (Cloudflare Pages env vars)
# NUXT_API_BASE=https://care.minhojan-world.site
# NUXT_COOKIE_SECURE=true

# LOCAL DEV — defaults are already dev-friendly:
#   apiBase    -> https://care.minhojan-world.site
#   cookieSecure -> false (so localhost:3000 can receive the cookie)
# Only override if you're running a local server:
# NUXT_API_BASE=http://localhost:8080
